# Runtime dependencies (pinned version ranges)
# See pyproject.toml for the authoritative dependency declarations.
dvc>=3.0,<4.0
fsspec>=2023.1.0

# Development / testing
pytest>=7.0
pytest-mock>=3.10
