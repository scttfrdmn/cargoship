---
gitlab_urls:
  api: https://gitlab.oit.duke.edu/api/v4/
  download: https://gitlab.oit.duke.edu
  use_package_registry: true
env:
  - VAULT_ADDR={{ .Env.VAULT_ADDR }}
  - TRANSIT_SECRET_ENGINE_PATH=ssi-systems/transit
  - VAULT_TOKEN={{ .Env.VAULT_TOKEN }}
builds:
  - env:
      - CGO_ENABLED=0
    main: ./cli
    mod_timestamp: '{{ .CommitTimestamp }}'
    flags:
      - -trimpath
    ldflags: |
      -s -w
      -X {{ .ModulePath }}/cli/cmd.date={{ .CommitTimestamp }}
      -X {{ .ModulePath }}/cli/cmd.version={{ .Tag }}
      -X {{ .ModulePath }}/cli/cmd.commit={{ .ShortCommit }}
    goos:
      - linux
      - darwin
    goarch:
      - amd64
    binary: 'suitcasectl'
archives:
  - format: tar.gz
    name_template: '{{ .ProjectName }}-{{ .Version }}_{{ .Os }}_{{ .Arch }}'
checksum:
  name_template: '{{ .ProjectName }}-{{ .Version }}_SHA256SUMS'
  algorithm: sha256
snapshot:
  name_template: "{{ .Tag }}-snapshot"
brews:
  - tap:
      owner: devil-ops
      name: homebrew-devil-ops
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    description: "Data Suitcase Suitcase Controller"
    name: "suitcasectl"
    homepage: https://gitlab.oit.duke.edu/devil-ops
    install: |
      bin.install "suitcasectl"
nfpms:
  - id: rpms
    license: MIT
    description: Package up research files for distribution
    vendor: Duke University
    section: admin
    priority: optional
    maintainer: Drew Stinnett <drew.stinnett@duke.edu>
    homepage: https://gitlab.oit.duke.edu/devil-ops/data-suitcase
    formats:
      - rpm
  - id: debs
    license: MIT
    description: Package up research files for distribution
    vendor: Duke University
    section: admin
    priority: optional
    homepage: https://gitlab.oit.duke.edu/devil-ops/data-suitcase
    maintainer: Drew Stinnett <drew.stinnett@duke.edu>
    formats:
      - deb
uploads:
  - name: productionrpm
    target: https://oneget.oit.duke.edu/rpm/devil-ops-rpms/
    method: PUT
    mode: archive
    username: api
    ids:
      - rpms
  - name: productiondeb
    target: https://oneget.oit.duke.edu/debian-packages/upload/devil-ops-debs/main/
    method: PUT
    mode: archive
    username: api
    ids:
      - debs
signs:
- cmd: cosign
  args: ["sign-blob", "--key=hashivault://package-signing", "--output-signature=${signature}", "${artifact}"]
  artifacts: all
